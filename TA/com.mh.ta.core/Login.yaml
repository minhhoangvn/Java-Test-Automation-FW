name: LoginDemo
threadCount: 10
parallel: tests
configFailurePolicy: skip
tests:
  - name: LOGIN CHROME
    parallel: tests
    parameters: { browser: chrome }
    classes:
      - com.mh.ta.login.TestLogin

  - name: LOGIN FIREFOX
    parallel: tests
    parameters: { browser: firefox }
    classes:
      - com.mh.ta.login.TestLogin

