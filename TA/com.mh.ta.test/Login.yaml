name: LoginDemo
threadCount: 10
parallel: tests
configFailurePolicy: skip
tests:
  - name: LOGIN CHROME
    parallel: tests
    parameters: { browser: chrome, configFileName: application }
    classes:
      - com.mh.ta.test.TestLogin

  - name: LOGIN FF
    parallel: tests
    parameters: { browser: chrome, configFileName: application }
    classes:
      - com.mh.ta.test.TestLogin