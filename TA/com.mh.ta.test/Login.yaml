name: LoginDemo
threadCount: 10
parallel: methods
configFailurePolicy: skip
tests:
  - name: LOGIN CHROME
    parallel: tests
    parameters: { browser: chrome, configFileName: application }
    classes:
      - com.mh.ta.guice.TestPerson

  - name: LOGIN FF
    parallel: tests
    parameters: { browser: chrome, configFileName: application1 }
    classes:
      - com.mh.ta.guice.TestPerson